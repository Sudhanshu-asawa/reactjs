import * as React from 'react';
import { creditMask } from './utils';
import InputComponent from './InputComponent';
const NumberField = (props) => {
    const handleKeyPress = (e) => {
        if (e.key === 'Enter')
            props.onNumberEdit(false);
    };
    return (React.createElement("div", { className: "component-atm-card-number", style: {
            fontSize: (props.numberFontSize ? props.numberFontSize : 25) * (props.scale ? props.scale : 1),
            fontFamily: props.fontFamily ? props.fontFamily : '',
            color: props.dataColor ? props.dataColor : ''
        } },
        !props.numberEdit ?
            React.createElement("div", { onClick: () => props.onNumberEdit(true) }, creditMask(props.number, props.hideDigits)
                .map((item, index) => React.createElement("div", { key: index }, item)))
            :
                React.createElement(InputComponent, { fontSize: (props.numberFontSize ? props.numberFontSize : 25) * (props.scale ? props.scale : 1), fontFamily: props.fontFamily ? props.fontFamily : '', value: props.number, onKeyPress: handleKeyPress, maxLength: 16, scale: props.scale, letterSpacing: `${4 * (props.scale ? props.scale : 1)}px`, type: "number", onBlur: () => props.onNumberEdit(false), onChange: (value) => props.onChange(value, 'number') }),
        props.numberEdit &&
            React.createElement("span", { style: {
                    fontSize: 10 * (props.scale ? props.scale : 1)
                } },
                props.number.length,
                " / 16")));
};
export default NumberField;
